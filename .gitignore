# Cooja/Contiki-NG build artifacts (regenerated by the script — don't commit)
contiki-ng/
scenarios/
logs/
results/
*.jar
*.class

# NOTE: data/ (real_cooja_results.csv, per_node_dataset.csv) IS committed —
# these are the paper's dataset outputs, not build artifacts.

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS/editor
.DS_Store
*.swp
